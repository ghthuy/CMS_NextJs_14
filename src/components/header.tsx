'use client'
import React from 'react';
import Profile from '@/components/ui/profile';

const Header: React.FC = () => {
    return <header><Profile />This is Header</header>;
};

export default Header;